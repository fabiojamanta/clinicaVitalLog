import { Component, OnInit } from '@angular/core';import { CommonModule } from '@angular/common';import { FormsModule } from '@angular/forms';import { ApiService } from '../../services/api.service';import { DateBrPipe } from '../../core/date-br.pipe';
@Component({selector:'app-lots',standalone:true,imports:[CommonModule,FormsModule,DateBrPipe],template:`<div class="top"><div class="page-title"><h1>Lotes</h1><p>Controle por lote, validade e saldo disponível.</p></div></div><div class="card grid grid-3"><div><label>Produto</label><select [(ngModel)]="form.product_id"><option [ngValue]="0">Selecione</option>@for(p of products;track p.id){<option [ngValue]="p.id">{{p.name}}</option>}</select></div><div><label>Fornecedor</label><select [(ngModel)]="form.supplier_id"><option [ngValue]="0">Selecione</option>@for(s of suppliers;track s.id){<option [ngValue]="s.id">{{s.name}}</option>}</select></div><div><label>Número do lote</label><input [(ngModel)]="form.lot_number"></div><div><label>Validade</label><input type="date" [(ngModel)]="form.expiration_date"></div><div><label>Estoque inicial</label><input type="number" [(ngModel)]="form.current_stock"></div><div class="form-actions"><button type="button" class="btn" (click)="save()">Salvar</button></div></div><div class="card table-wrap"><table><tr><th>Produto</th><th>Fornecedor</th><th>Lote</th><th>Validade</th><th>Estoque</th><th>Status</th></tr>@for(i of rows;track i.id){<tr><td>{{i.product_name}}</td><td>{{i.supplier_name}}</td><td>{{i.lot_number}}</td><td>{{i.expiration_date | dateBr}}</td><td>{{i.current_stock}}</td><td><span class="badge" [class.danger]="i.expired" [class.warn]="i.near_expiration&&!i.expired" [class.ok]="!i.near_expiration&&!i.expired">{{i.expired?'Vencido':(i.near_expiration?'A vencer':'OK')}}</span></td></tr>}</table></div>`})
export class LotsComponent implements OnInit{rows:any[]=[];products:any[]=[];suppliers:any[]=[];form:any={product_id:0,supplier_id:0,lot_number:'',expiration_date:'',current_stock:0,quantity_in_use:0,blocked:false,active:true};constructor(private api:ApiService){}ngOnInit(){this.load();this.api.get<any[]>('/products').subscribe(r=>this.products=r);this.api.get<any[]>('/suppliers').subscribe(r=>this.suppliers=r)}load(){this.api.get<any[]>('/lots').subscribe(r=>this.rows=r)}save(){this.api.post('/lots',this.form).subscribe(()=>{this.form={product_id:0,supplier_id:0,lot_number:'',expiration_date:'',current_stock:0,quantity_in_use:0,blocked:false,active:true};this.load()})}}
