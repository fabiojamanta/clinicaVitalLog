import { Component, OnInit } from '@angular/core';import { CommonModule } from '@angular/common';import { FormsModule } from '@angular/forms';import { ApiService } from '../../services/api.service';
@Component({selector:'app-product-suppliers',standalone:true,imports:[CommonModule,FormsModule],template:`<div class="top"><div class="page-title"><h1>Produto x Fornecedor</h1><p>Vincule quais fornecedores entregam cada produto.</p></div></div><div class="card grid grid-3"><div><label>Produto</label><select [(ngModel)]="form.product_id"><option [ngValue]="0">Selecione</option>@for(p of products;track p.id){<option [ngValue]="p.id">{{p.name}}</option>}</select></div><div><label>Fornecedor</label><select [(ngModel)]="form.supplier_id"><option [ngValue]="0">Selecione</option>@for(s of suppliers;track s.id){<option [ngValue]="s.id">{{s.name}}</option>}</select></div><div><label>Código no fornecedor</label><input [(ngModel)]="form.supplier_code"></div><div class="form-actions"><button type="button" class="btn" (click)="save()">Salvar</button></div></div><div class="card table-wrap"><table><tr><th>Produto</th><th>Fornecedor</th><th>Código</th></tr>@for(i of rows;track i.id){<tr><td>{{i.product_name}}</td><td>{{i.supplier_name}}</td><td>{{i.supplier_code}}</td></tr>}</table></div>`})
export class ProductSuppliersComponent implements OnInit{rows:any[]=[];products:any[]=[];suppliers:any[]=[];form:any={product_id:0,supplier_id:0,supplier_code:'',notes:'',active:true};constructor(private api:ApiService){}ngOnInit(){this.load();this.api.get<any[]>('/products').subscribe(r=>this.products=r);this.api.get<any[]>('/suppliers').subscribe(r=>this.suppliers=r)}load(){this.api.get<any[]>('/product-suppliers').subscribe(r=>this.rows=r)}save(){this.api.post('/product-suppliers',this.form).subscribe(()=>{this.form={product_id:0,supplier_id:0,active:true};this.load()})}}
